on:
  workflow_dispatch:
    inputs:
      input1:
        type: string
        default: "default val"
        required: true

jobs:
  job-name:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'done'
